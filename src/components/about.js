import React, { useContext, useEffect} from 'react'
// import noteContext from '../context/note/noteContext'
export default function About() {
    // let a = useContext(noteContext)
    // useEffect(() =>{
    // }, [])

  return (
    <>
    {/* <div>The name is: {a.s1.name}</div> */}
    <div>The name is: soham</div>

    {/* <div>The name is: {a.name}</div> when we didn't nade the usestate hook and just assigned the name and class to s1 ={ "name": "Soham", "class": "5b" } we can access it as a.name */}

    </>
  )
}
